// Database: gurubot
// Table: managedvoicechannels
// Column: channelId (text)
// Column: guildId (text)
// Column: id (uuid)

// Create a query to connect to the database and ensure the table exists
// Create a query to get, insert, and delete managed voice channels
// Create a query to get managed voice channels by server

// Table: logchannels
// Column: channelId (text)
// Column: guildId (text)
// Column: id (uuid)

// Create a query to connect to the database and ensure the table exists
// Create a query to get, insert, and delete log channels
// Create a global list of log channels to ignore when sending messages